# Kali base image digest pin — consumed by docker/kali/Dockerfile via build-arg.
# Updates require security-reviewer approval (see CODEOWNERS).
# Path: docker/kali/kali.env (non-dotfile per workspace permissions; the plan
# references this as `.env.kali` and the CODEOWNERS pattern docker/kali/**
# covers both names).

# Live kalilinux/kali-rolling digest. Captured 2026-05-26 via
#   docker pull kalilinux/kali-rolling
#   docker image inspect kalilinux/kali-rolling:latest \
#     --format '{{index .RepoDigests 0}}'
# Rotation is gated by CODEOWNERS on docker/kali/** (security-reviewer).
KALI_DIGEST=40e386ce0c263b924e59215a3a2a8d969a95d808856ed255d0b1ee95fbfec2a4
